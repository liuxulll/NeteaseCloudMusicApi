{
  "version": 2,
  "builds": [
    {
      "src": "/index.php",
      "use": "vercel-php@0.6.0",
      "config": {
        "installCommand": "apt-get update && apt-get install -y libssl1.0.0"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.php?$1"
    }
  ]
}
